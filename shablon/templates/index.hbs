<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        {{#if title }}
       {{{title}}}
       {{else }}
       Нащваниие?
        {{/if}}
        </title>
</head>
<body>
    <h1> Hello , {{{ buddy }}}</h1>
    <h2> Your {{{surname}}} , {{{age}}} , {{{height}}} </h2>
    <p> Шваксму хватает только на; </p>
    <ul>
        {{#each list }}
        <li> {{this}}</li>
        {{/each}}
    </ul>
    <ul>
{{#each keys}}
<li>{{@key}} - {{this}}</li>

{{/each}}

    </ul>
</body>
</html>